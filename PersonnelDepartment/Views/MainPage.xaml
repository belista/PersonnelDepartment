<Page x:Class="PersonnelDepartment.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      xmlns:models="clr-namespace:PersonnelDepartment.Models"
      ShowsNavigationUI="False"
      xmlns:controls="clr-namespace:PersonnelDepartment.Controls"
      x:Name="View">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Background="#ddd">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <Rectangle Fill="#c6c6c6" />
            <Button Content="Экс"
                    Height="50"
                    Command="{Binding ExportCommand}"
                    Grid.Row="1" />
            <Button Content="Имп"
                    Height="50"
                    Command="{Binding ImportCommand}"
                    Grid.Row="2" />
            <Grid Grid.Column="1"
                  Margin="10 0 0 0"
                  Grid.RowSpan="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="60" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            Width="305">
                    <TextBlock Text="Работники"
                               FontSize="25"
                               VerticalAlignment="Center" />
                    <Button Content="Добавить"
                            Height="20"
                            Command="{Binding AddCommand}" />
                </StackPanel>
                <TextBox Width="280"
                         HorizontalAlignment="Left"
                         VerticalAlignment="Center"
                         Height="28"
                         BorderThickness="2"
                         BorderBrush="Gray"
                         Background="LightGray"
                         FontSize="17"
                         Grid.Row="1"
                         Text="{Binding QueryString, UpdateSourceTrigger=PropertyChanged}" />

                <ListView x:Name="EmployeeList"
                          ItemsSource="{Binding Employees}"
                          SelectedItem="{Binding SelectedEmployee, Mode=TwoWay}"
                          Grid.Row="2"
                          BorderThickness="0"
                          Background="Transparent"
                          FontSize="18"
                          HorizontalContentAlignment="Left">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        Grid.Row="1">
                                <TextBlock Text="{Binding FullName}"
                                           Margin="2"
                                           TextWrapping="Wrap" />
                                <TextBlock Text="{Binding RegistrationNumber}"
                                           Margin="2"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Удалить"
                                      Command="{Binding RemoveCommand}" />
                        </ContextMenu>
                    </ListView.ContextMenu>
                </ListView>
            </Grid>
        </Grid>


        <Grid x:Name="PreloaderHost"
              Grid.Column="1">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Viewbox Width="200"
                         Height="200">
                    <Image Source="pack://application:,,,/Assets/smile.png" />
                </Viewbox>
                <TextBlock Text="Ни одна запись не выбрана :("
                           TextAlignment="Center"
                           FontSize="32"
                           Margin="0, 24, 0, 0" />
            </StackPanel>
        </Grid>
        <ContentPresenter x:Name="ContentPresenter"
                          Content="{Binding ElementName=EmployeeList, Path=SelectedItem}"
                          Grid.Column="1">
            <ContentPresenter.Style>
                <Style TargetType="ContentPresenter">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=EmployeeList, Path=SelectedItem}"
                                     Value="{x:Null}">
                            <DataTrigger.Setters>
                                <Setter Property="Visibility">
                                    <Setter.Value>
                                        <Visibility>Collapsed</Visibility>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger.Setters>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentPresenter.Style>
            <ContentPresenter.ContentTemplate>
                <DataTemplate>
                    <ScrollViewer Background="White">
                        <StackPanel d:DataContext="{d:DesignInstance Type=models:Employee}">

                            <controls:LabelTextBox Key="Фамилия"
                                                   Value="{Binding FirstSurname}" />
                            <controls:LabelTextBox Key="Вторая фамилия"
                                                   Value="{Binding SecondSurname}" />
                            <controls:LabelTextBox Key="Третья фамилия"
                                                   Value="{Binding ThirdSurname}"/>
                            <controls:LabelTextBox Key="Имя"
                                                   Value="{Binding Name}" />
                            <controls:LabelTextBox Key="Отчество"
                                                   Value="{Binding Patronymic}" 
                                                   VisibilityBorder="Visible"/>
                            <controls:LabelTextBox Key="Паспортные данные"
                                                   Value="{Binding PassportInfo}" />
                            <controls:LabelTextBox Key="Проживание"
                                                   Value="{Binding Registration}" />
                            <controls:LabelTextBox Key="ИНН"
                                                   Value="{Binding RegistrationNumber}" />
                            <controls:LabelTextBox Key="Телефон"
                                                   Value="{Binding Phone}" 
                                                   VisibilityBorder="Visible"/>
                            <controls:LabelTextBox Key="Должность"
                                                   Value="{Binding FirstPosition}" />
                            <controls:LabelTextBox Key="Вторая должность"
                                                   Value="{Binding SecondPosition}" />
                            <controls:LabelTextBox Key="Третья должность"
                                                   Value="{Binding ThirdPosition}" />
                            <controls:LabelTextBox Key="Первый приказ"
                                                   Value="{Binding FirstOrder}" />
                            <controls:LabelTextBox Key="Второй приказ"
                                                   Value="{Binding SecondOrder}" />
                            <controls:LabelTextBox Key="Дополнительно"
                                                   Value="{Binding Additionally}"/>
                            <!--<TextBox Text="{Binding FirstSurname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding SecondSurname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding ThirdSurname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding Patronymic, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <DatePicker SelectedDate="{Binding DateOfBirth, Mode=TwoWay}" />
                            <TextBox Text="{Binding PassportInfo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding Registration, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding RegistrationNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding FirstPosition, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding SecondPosition, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Text="{Binding ThirdPosition, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <DatePicker SelectedDate="{Binding EmploymentDate, Mode=TwoWay}" />
                            <TextBox Text="{Binding FirstOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <CheckBox IsChecked="{Binding Dismissed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <DatePicker SelectedDate="{Binding DateOfDismissal, Mode=TwoWay}" />
                            <TextBox Text="{Binding SecondOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="{Binding WorkDays}"
                                       Margin="8" />
                            <TextBox Text="{Binding Additionally, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />-->

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="25" />
                                    <ColumnDefinition Width="190" />
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="190" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="20"/>
                                </Grid.RowDefinitions>

                                <Button Content="Сохранить"
                                        Command="{Binding ElementName=View, Path=DataContext.SaveCommand}"
                                        CommandParameter="{Binding}"
                                        Grid.Column="1"
                                        Height="30" 
                                        Grid.Row="0"/>

                                <Button Content="Отмена"
                                        Grid.Column="3"
                                        Command="{Binding ElementName=View, Path=DataContext.CancelCommand}"
                                        CommandParameter="{Binding}" 
                                        Grid.Row="0"/>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </DataTemplate>
            </ContentPresenter.ContentTemplate>
        </ContentPresenter>
    </Grid>
</Page>
